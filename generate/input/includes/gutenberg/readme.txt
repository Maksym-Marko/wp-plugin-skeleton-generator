Here are some pre-built Gutenberg blocks. You can use them as is or customize each one individually.
To configure the blocks, you need to open a terminal in the gutenberg folder and run the following commands:
- npm install

To live code compiling use this command:
- npm run start

If you want simply build your code, use this command:
- npm run build